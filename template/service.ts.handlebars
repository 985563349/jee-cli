import { request } from '@umijs/max';

export type {{name}}Model = {
  id: string;
};

export type {{name}}Query = {
  id?: string;
};

export const fetch{{name}}List = (params: {{name}}Query) => {
  return request<{data: {{name}}Model[]}>('', {
    method: 'GET',
    params,
  });
};

export const create{{name}} = (data: Partial<{{name}}Model>) => {
  return request('', {
    method: 'POST',
    data,
  });
};

export const update{{name}} = (id: string, data: Partial<{{name}}Model>) => {
  return request('', {
    method: 'PATCH',
    data,
  });
};

export const delete{{name}} = (id: string) => {
  return request('', {
    method: 'DELETE',
  });
};
