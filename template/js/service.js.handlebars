import { request } from '@/utils';

export const get{{name}}s = (params) => {
  return request({
    url: '',
    method: 'GET',
    params,
  });
};

export const create{{name}} = (data) => {
  return request({
    url: '',
    method: 'POST',
    data,
  });
};

export const update{{name}} = (id, data) => {
  return request({
    url: '',
    method: 'PATCH',
    data,
  });
};

export const delete{{name}} = (id) => {
  return request({
    url: '',
    method: 'DELETE',
  });
};
