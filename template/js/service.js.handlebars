import { request } from '@umijs/max';

export const fetch{{name}}List = (params) => {
  return request('', {
    method: 'GET',
    params,
  });
};

export const create{{name}} = (data) => {
  return request('', {
    method: 'POST',
    data,
  });
};

export const update{{name}} = (id, data) => {
  return request('', {
    method: 'PATCH',
    data,
  });
};

export const delete{{name}} = (id) => {
  return request('', {
    method: 'DELETE',
  });
};
